import 'package:flutter/material.dart';

class StatData {
  final String name;
  final double amount;
  final Color color;
  final IconData icon;

  StatData(this.name, this.amount, this.color, this.icon);
}